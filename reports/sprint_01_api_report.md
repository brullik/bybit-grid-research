# Sprint 01 API Report

Run UTC: pending runtime execution

## env
mainnet by default, configurable via `.env`.

## public API status
Implemented. Local execution environment returned proxy 403 when attempting Bybit public API, so live public API status must be verified from a network that can reach `https://api.bybit.com`.

## linear instruments
Generated by `scripts/smoke_public_api.py` into `data/metadata/instruments_linear.parquet`.

## Trading symbols
Reported by `scripts/smoke_public_api.py`; PreLaunch symbols are not part of the V1 trading universe report.

## sample data coverage
Generated by `scripts/download_sample_data.py --symbols BTCUSDT ETHUSDT --days 7` for klines, mark klines, and funding history.

## gap summary
Generated as `data/quality/gap_report_*.parquet` by sample download script.

## account info summary
Generated by `scripts/smoke_private_account.py` when API credentials are configured; skipped with a clear message otherwise.

## grid validate status
Validate-only scaffold implemented and disabled by default with `GRID_VALIDATE_ENABLED=false`. No create/close implementation is available in Sprint 01.

## blockers
Bybit API reachability depends on local network/proxy. No code-level blockers found in pytest.

## next sprint recommendation
Run public/private smoke scripts from the target Windows machine, inspect saved Parquet and redacted JSON, then proceed to research-only data quality expansion before any strategy implementation.
