Never edit pm_acceptance/** in an implementation PR.
Never edit .github/workflows/pm-acceptance.yml in an implementation PR.
Never edit docs/frozen_contracts/**, AGENTS.md, .github/CODEOWNERS, scripts/check_protected_paths.py, or scripts/check_task_scope.py in an implementation PR.
Never delete, weaken, skip, xfail, parameter-pad, rename, or replace a frozen acceptance test.
Never use pytest.raises(Exception) or pytest.raises(BaseException).
Never catch Exception or BaseException in tests unless a frozen PM contract explicitly requires it.
Never use a passing test whose only assertion checks a label, test ID, type name, nonempty list, generic object existence, or missing-path failure unrelated to the behavior name.
Implementation PRs may change only paths allowed by pm_acceptance/active_task.json.
Implementation authority is offline-only by default: no network, private API, credentials, live execution, Telegram, order, position, wallet, or native-grid mutation. Path scope alone never authorizes these actions. They remain forbidden unless a base-controlled staged-execution contract explicitly authorizes the current stage and freezes fail-closed risk, credential, approval, kill-switch, and rollback gates.
Never place credentials in repository files, issues, pull requests, comments, logs, tests, fixtures, reports, or agent context; approved runtime secret stores are the only credential boundary.
Withdrawal and ordinary-order mutation are never authorized in V1; execution scope is limited to the native Bybit grid lifecycle after its future staged gates exist.
Run the exact commands from the active task and keep the PR draft until all required acceptance checks and lifecycle evidence pass.
Under standing owner authorization, the autonomous maintainer may mark its own non-probe PR ready and merge it only when the expected head SHA is unchanged, every required check is successful, the exact scope is verified, and no review thread is unresolved. Mandatory RED probes must use a `probe/` branch and must always be closed unmerged. Never bypass a required check, force a merge, or merge an unknown, pending, stale, or failing status.
If a frozen acceptance test appears wrong, report it and stop the active task; do not change it in an implementation, task-definition, or ordinary control-plane PR. A frozen test may be corrected only through the one-time `pm-frozen-erratum` procedure after explicit owner authorization: first close the invalid active task, then merge the base-owned erratum control plane, then use an owner-authored PR that reactivates that same task and changes exactly `pm_acceptance/active_task.json` plus the two-file erratum payload (one existing `test_*.py` and a new canonical `pm_acceptance/errata/<task_id>.json`). The manifest must identify an ancestor commit whose exact canonical active-task bytes equal the reactivated head bytes, SHA-256-pin those head bytes and the base and head test bytes, and declare the exact expected RED passed and failed node IDs. Every `test_*` function AST and the baseline collected node-ID set must remain identical; deleting, weakening, skipping, xfail-padding, parameter-padding, renaming, or replacing a test remains forbidden. The erratum PR must produce the exact manifest-declared RED profile over exactly the baseline node-ID set with no skipped, xfailed, xpassed, deselected, collection-error, setup-error, or teardown-error outcomes. Baseline collection may use trusted base dependencies, but corrected-head execution for every erratum version must use the normal isolated `$RUNNER_TEMP` import order with its empty/staged `scripts` package and no full base/head scripts or ordinary tests on `PYTHONPATH`. After it merges, discard all prior probe or implementation evidence and repeat a fresh mandatory `probe/` RED closure, fresh-main implementation PR, and separate task-close PR.
If and only if that task's already-merged v1 erratum fixture is itself proven invalid, report it and stop the reactivated task; never edit or replace the v1 manifest. After explicit owner authorization, a separate base-owned control-plane revision may enable the one-and-only `pm_frozen_erratum_v2` recovery. First close the task again. The owner-authored v2 repair then reactivates the identical historical task and changes exactly three paths: `pm_acceptance/active_task.json`, the same existing frozen `test_*.py`, and a new canonical `pm_acceptance/errata/<task_id>.v2.json`. The v2 manifest must SHA-256-chain to the immutable `pm_frozen_erratum_v1` manifest, identify an ancestor predecessor commit containing those exact v1 manifest bytes, the v1-corrected base test bytes, and the reactivated active-task bytes, and pin the new corrected test and exact RED outcomes. The current base node IDs must be collected using trusted base dependencies; the v2 head must run with the normal base-isolated `$RUNNER_TEMP` import order and its empty/staged `scripts` package. Every collected node must have exactly one plain call-phase pass or failure matching the manifest; all collection, deselection, skip, xfail/xpass, duplicate, missing-call, setup, and teardown anomalies fail closed. The v1 procedure remains unchanged, v2 cannot be reused, and no v3 or unmanifested repair exists. A successful v2 repair also invalidates every prior lifecycle artifact and requires a new closed-unmerged RED probe, fresh-main implementation PR, and separate task-close PR.
Never edit dependency/config files pyproject.toml, requirements.txt, requirements-dev.txt, requirements/*.txt, uv.lock, poetry.lock, Pipfile, or Pipfile.lock in any PR for control-plane v1.
The one-time `pm-recovery-bundle` is limited to `p0-recovery-walk-forward-committed-key` and tasks #156 and #157. Its owner-authored activation must open Draft, carry exactly that label, and be one non-merge commit directly on canonical live `main`, changing only `pm_acceptance/active_task.json` and the new canonical reactivation manifest. It may move from Draft to Ready only through the owner's `ready_for_review` event after both exact-SHA Draft acceptance components pass; a fresh unchanged-SHA Ready run is required before merge. Every initial run and rerun must validate `github.triggering_actor` and `github.run_attempt`, and combined RED must execute a complete immutable commit-staged head tree whose regular-file bytes and modes are rechecked immediately before and after execution. The isolated child must not process editable-install path files or inherit checkout import paths, and every imported `bybit_grid` or `scripts` module must resolve inside that staged tree. It may activate only the exact union of the historical eight-path and four-path scopes after every pinned task, test, helper, contract, history, suspension, and #156 v1-erratum predecessor check passes. Combined RED is exactly 52 plain call failures with the member sentinels. It is single-use: substitution, repetition, replay, v3, and unmanifested repair are forbidden. Rollback uses the ordinary inactive-task control plane and grants no live authority.
